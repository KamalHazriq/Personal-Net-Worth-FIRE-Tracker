# Copy this file to ".env" (same folder) and add your key to enable the AI Assistant.
# The key is read ONLY by the local backend and is never exposed to the browser.
# The rest of the app works fully offline without it.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the API server port (default 8787).
# SERVER_PORT=8787
